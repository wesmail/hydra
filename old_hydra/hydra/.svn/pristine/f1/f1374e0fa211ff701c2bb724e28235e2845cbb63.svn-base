#include "hmdcconfig.h"

ClassImp(HMdcConfig)

HMdcConfig::HMdcConfig(const Char_t* name,const Char_t* title)
    :TNamed(name,title)
{
    setScale(1000);
}
HMdcConfig::HMdcConfig()
{
  setScale(1000);
}
HMdcConfig::~HMdcConfig(){}
